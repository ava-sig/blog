# ==== Core Secrets ====
# Change these in your local .env
POSTGRES_USER=blog
POSTGRES_PASSWORD=blogpw
POSTGRES_DB=blog
# Host port that maps to container 5432
POSTGRES_PORT=5433

# Sign your JWTs with this secret (use a strong random string in your .env)
JWT_SECRET=change_me_in_local_env

# ==== Networking ====
# Frontend origin allowed by the API CORS
CORS_ORIGIN=http://localhost:5000
# Public base for uploaded file URLs exposed by the API service
PUBLIC_BASE_URL=http://localhost:3000
# Nuxt frontend will call the API at this base URL
NUXT_PUBLIC_API_BASE=http://localhost:3000
# Optional: Fallback social image used when a post has no image
NUXT_PUBLIC_SOCIAL_FALLBACK=

# ==== E2E (Playwright) ====
# Base URL where the Docker-exposed API is reachable from the host
# Used by Playwright tests and by Nuxt (via NUXT_PUBLIC_API_BASE when running E2E)
API_BASE=http://localhost:3388
# Port for the Nuxt dev server started by Playwright (configurable to avoid conflicts)
FRONTEND_PORT=5999
